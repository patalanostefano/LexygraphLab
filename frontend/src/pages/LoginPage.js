import React from 'react';
import { Box, Typography } from '@mui/material';

// Questo è un componente di login semplificato da sostituire con il tuo
function LoginPage() {
  return (
    <Box display="flex" justifyContent="center" alignItems="center" height="100vh">
      <Typography variant="h4">Pagina di Login (placeholder)</Typography>
      <Typography variant="body1">
        Qui verrà integrata la tua pagina di login esistente
      </Typography>
    </Box>
  );
}

export default LoginPage;
